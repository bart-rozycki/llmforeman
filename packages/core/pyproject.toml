[project]
name = "llmforeman-core"
version = "0.0.0"
description = "LLMForeman core: product/domain/orchestration runtime (provider- and runtime-agnostic)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/llmforeman_core"]
