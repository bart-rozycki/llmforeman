# llmforeman-runtimes

Local inference runtime integrations for LLMForeman (e.g. Ollama, MLX,
llama.cpp).

Boundary only for now: no runtime clients are declared or implemented. A local
*runtime* is intentionally distinct from a cloud *provider*
(`llmforeman-providers`).
