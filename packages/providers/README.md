# llmforeman-providers

Cloud LLM provider integrations for LLMForeman (e.g. Anthropic, OpenAI,
Gemini).

Boundary only for now: no provider SDKs are declared or implemented. A cloud
*provider* is intentionally distinct from a local inference *runtime*
(`llmforeman-runtimes`).
