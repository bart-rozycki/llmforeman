# llmforeman-cli

Thin command-line interface into the LLMForeman Python runtime.

The CLI is an interface, not a parallel backend. It currently exposes a single
`llmforeman` console entry point that is a no-op placeholder; no real commands
are implemented yet.
